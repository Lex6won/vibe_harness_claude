# 02 설계서 — {프로젝트명}

- 작성: solution-architect (규칙 자동결정, 사용자에게 기술 안 물음) · 작성일: {날짜}

## 결정 요약
| 항목 | 결정 | 근거(규칙) |
|---|---|---|
| Track | {local-automation/streamlit-internal/fastapi-web/react-fastapi/node-api} | {approved-tracks + deploy-context} |
| 인증 | {keycloak-oidc / citizen+admin-idpw} | {존별} |
| gvskb 프로파일 | {internal-db-query/web-civil-service/civil-complaint-chatbot/public-default-strict} | {존·용도} |

## 화면 흐름
1. {화면} → {화면}
2. {…}

## DB 스키마 (PostgreSQL 16)
```sql
-- 프로젝트별 분리, 개인정보 컬럼은 red 패턴 회피/암호화
CREATE TABLE {테이블} (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  ...
);
```
- 승인 확장만 사용: {pgcrypto/pg_trgm/uuid-ossp/pgvector}

## 외부 의존 처리
- {외부 API/LLM/CDN}: {행정망→망연계·self-host·사전반입 / 외부망→통제된 허용}
- 대민이 내부자료 참조 시: 단방향 사전반입 설계(직결 금지)

## 제약 반영 (runtime-env)
- 런타임 {Python 3.12/Node 20/PG16}, 쿼터 1C/2G, base_path `/apps/{project}/`, `/health`, journald

## 미승인 요청 처리 (있었다면)
- 요청: {스택} → 선회: {가까운 승인 Track + 방법}

---
※ 기술 선택지를 사용자에게 나열하지 않았고, 결정 결과만 한 줄로 통지함.
